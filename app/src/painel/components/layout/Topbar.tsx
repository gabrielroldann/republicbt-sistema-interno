/**
 * Removido: o cabeçalho virou parte de `TopNav.tsx`, que reúne marca,
 * navegação, perfil e o cluster de status numa única peça.
 * Mantido vazio como marcador da decisão; pode ser apagado.
 */
export {};
